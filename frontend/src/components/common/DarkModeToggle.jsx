// components/common/DarkModeToggle.jsx
const DarkModeToggle = ({ toggle }) => (
  <button onClick={toggle} className="border px-3 py-1 rounded text-sm">
    Toggle Theme
  </button>
);

export default DarkModeToggle;
